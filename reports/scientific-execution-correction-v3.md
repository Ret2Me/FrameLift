# Startup synchronization correction and retest v3

The user requested more liberal limits, execution fixes, and continued testing. The v2 retest started at 2026-09-07 17:09:37 UTC with independently tested 2 GiB / 512-task per-unit limits and four workers. It exposed an additional startup failure before a normal process receipt could be retained: unit `87f2b8db38f02d5ec64c9a8c5194a4c8650491b6234e3de295761e3ef68e2dbe`, candidate, 9,600 baud, observation 4491, repeat-a, reported `systemd did not materialize the required per-job cgroup` after about 0.4 seconds. Its output directory is empty. Because the old exception path did not retain startup stderr/return code, the exact historical mechanism is not established as a benign fast-exit race.

The v2 service was interrupted to address this infrastructure failure. Its automatic terminal report retains 43 normalized attempts: 42 completed and one supervisor failure. The report remains incomplete; interrupted units are not treated as decoded zeros. All original v1/v2 inputs, configuration, code, receipts and failures remain preserved. The v2 process scopes were checked after interruption: none remained active.

V3 adds an explicit silent pre-execution gate outside the receiver sandbox. The target command cannot run until its supervisor has observed the cgroup, read back hard limits, and acquired baseline accounting. The gate then replaces itself with the unchanged receiver argv and restores stdin to /dev/null. The gate implementation/interpreter are part of execution provenance. Failure before release cannot execute the receiver, and startup failure diagnostics are retained. The previous resource-monitored natural-exit fix remains in place.

The same limits and scientific comparison apply: 2 GiB decoder cgroup memory, 512 tasks, 32 GiB virtual address space, four workers, 15-second termination grace and 3-second natural-exit grace. The coordinator has a separate 4 GiB ceiling; the sum of decoder memory ceilings is 8 GiB. The 30 IQ sources, driver code, candidate configuration, four rates, two repeats, null controls and seeds, normalizer, integrity checks and numerical scorer are unchanged. All 6,168 units are fresh in `campaign-v4-scientific-run-v3`.

This is another explicitly post-outcome operational retest, not a pristine holdout. No old failure is reclassified; no unvalidated mission PDU is promoted. Real mission repeat discrepancies and the original sensitivity exposure threshold remain visible. Independent regression, actual user-service synthetic integration and startup stress must pass before the new amendment is timestamped and the full series is launched. The separate v3 ExecStopPost reporter will automatically retain the terminal recount without invoking decoders or claiming an independent CRC rerun.
